- @page_title = t('shoppe.gifts.gifts')

= content_for :header do
  %p.buttons
    = link_to t('shoppe.subscribers.subscribers') , [@gift, :subscribers], :class => 'button green'
    = link_to t('shoppe.gifts.back'), :gifts, :class => 'button'
  %h2.orders= t('shoppe.gifts.gifts')

= render 'form'

- @page_title = t('shoppe.gifts.subscriber_gifts')

= content_for :header do
  %p.buttons
    = link_to t('shoppe.gifts.back'), :subscriber_gifts, :class => 'button'
  %h2.orders= t('shoppe.gifts.subscriber_gifts')

= render 'form'
